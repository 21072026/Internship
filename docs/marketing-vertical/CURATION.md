# Devralınan backlog'un küratörlüğü

`21072026/Marketing` repo'sundan gelen **89 görevin tamamı**, bu repoda hâlihazırda
var olana karşı denetlendi. Her görev üç karardan birini aldı:

- **KEEP** — gerçekten yeni marketing işi, olduğu gibi taşınır.
- **ADAPT** — hâlâ isteniyor ama dikey mimarisi şeklini değiştiriyor; ne olacağı yazılı.
- **DROP** — bu repoda zaten var; gereksiz kılan dosya adıyla belirtilmiş.

**Sonuç: KEEP 4 · ADAPT 46 · DROP 39** (toplam 89 görev, 9 epic)

Görevlerin %44'ü gereksiz çıktı. 120 issue'yu körü körüne kopyalamak, bu repo'nun
backlog'unu zaten çözülmüş işlerle dolduracaktı — küratörlüğün asıl kazancı bu.

## Epic özeti

| Epic | Karar | Görev | KEEP | ADAPT | DROP |
| --- | --- | --- | --- | --- | --- |
| [epic-01](backlog/epic-01-import.md) Import the existing customer base | CARRY_OVER | 10 | 1 | 5 | 4 |
| [epic-02](backlog/epic-02-trials.md) Never lose a trial | CARRY_OVER | 11 | 0 | 8 | 3 |
| [epic-03](backlog/epic-03-analytics.md) See the funnel (analytics) | MOSTLY_REDUNDANT | 13 | 0 | 7 | 6 |
| [epic-04](backlog/epic-04-i18n.md) TR/DE/EN i18n | MOSTLY_REDUNDANT | 10 | 0 | 3 | 7 |
| [epic-05](backlog/epic-05-operations.md) Operations — Deploy it and keep it running | MOSTLY_REDUNDANT | 9 | 0 | 1 | 8 |
| [epic-06](backlog/epic-06-access.md) Access / roles — Access control and data protection | MOSTLY_REDUNDANT | 8 | 1 | 6 | 1 |
| [epic-07](backlog/epic-07-workflow.md) Workflow — daily work in the CRM | MOSTLY_REDUNDANT | 13 | 1 | 6 | 6 |
| [epic-08](backlog/epic-08-quality.md) Quality — Test coverage and guardrails | MOSTLY_REDUNDANT | 7 | 0 | 3 | 4 |
| [epic-09](backlog/epic-09-product-signals.md) Signals from the SaleVali product / Ürün sinyalleri | CARRY_OVER | 8 | 1 | 7 | 0 |


## epic-01 — Import the existing customer base

**Karar: CARRY_OVER**

The epic's *machinery* is almost entirely redundant: `/home/user/internship/src/lib/importPreview.ts` is already the one shared parse → validate → resolve → dry-run → apply engine (delimiter sniffing, BOM/CRLF/quoted-newline parser, `CREATE|UPDATE|UNCHANGED|SKIP|ERROR` vocabulary, chunking, and the explicit rule that "a dry run is the same call with a writer that does not write"), `/home/user/internship/src/lib/rosterIngest.ts` already owns idempotency + resume, `/home/user/internship/src/app/api/admin/import/route.ts` plus the CSV panel in `/home/user/internship/src/app/admin/settings/page.tsx` is already an ADMIN-only upload page with a dry-run preview and an e2e (`/home/user/internship/e2e/import-dryrun.spec.ts`), and `/home/user/internship/scripts/import-csv.mjs` is already a dry-run-by-default CLI with `--apply`/`--owner`. What is NOT redundant is the marketing domain itself: the destination has no account/customer record for a merchant (no `vatId`, no `country`, no `mrr`, no stage on `Company` — `/home/user/internship/prisma/schema.prisma:1267`), no sales-channel model, and no CSV column contract, so the epic's goal (get the real SaleVali spreadsheet into the CRM) is still unmet. Note the "no data migration" decision concerns the old marketing *database* (demo data); the spreadsheet of real customers is a separate, still-valid source, which is why this epic survives at all.

### KEEP (1)

- **T-1.1.7 · Import the channels each merchant sells on**  
  The one genuinely new table in the epic. Nothing in the destination models which marketplaces, shops and carriers a merchant runs — grep for vatId/MRR/channel over /home/user/internship/src and prisma/schema.prisma returns nothing relevant. Build it in the destination's established shape rather than the source's: copy `CompanyEntitlement` (schema.prisma:1303 — `@@unique([companyId, feature])`, key "stored as text so adding features needs no migration") instead of a Prisma `IntegrationChannel` enum, so adding OTTO or Kaufland is a code change and not a `db push` that the destructive-schema guard has to adjudicate. Keep the per-row `status` field (INTERESTED/…) — that is what rules out reusing the generic `Tag`/`UserTag` pair. Upsert on `[companyId, channel]` so a re-run is a no-op; an unknown channel name is a row-level ERROR with the valid list, never a silent skip.

### ADAPT (5)

- **T-1.1.1 · Define the CSV column contract and a sample fixture**  
  Still needed — no account-import contract exists in the destination — but every field it names has moved. `LifecycleStage` (PROSPECT_100…CHURNED_900) no longer exists: the MARKETING funnel is PipelineStage rows seeded from `MARKETING_FUNNEL` in /home/user/internship/src/lib/programTemplates.ts:517 with keys LEAD_NEW, LEAD_CONTACTED, LEAD_QUALIFIED, DEAL_PROPOSAL, DEAL_NEGOTIATION, DEAL_WON, DEAL_LOST. `Customer` becomes `Company` (/home/user/internship/prisma/schema.prisma:1267 — already has orgId, name, industry, address, contactEmail, size, and is registered in TENANT_MODELS at /home/user/internship/src/lib/orgContext.ts:66). The contract also gains a tenant column decision (the import runs for ONE orgId, never a column) and drops `assigned marketer e-mail` as a marketing-specific role — owner is any existing User in the org, because the Role enum is frozen. Document at docs/marketing-import.md next to docs/roster-feed.md, fixture at scripts/fixtures/.

- **T-1.1.4 · Idempotent matching: VAT ID first, then company + country**  
  The idempotency PROTOCOL exists and must be reused, not rebuilt: /home/user/internship/src/lib/rosterIngest.ts already guarantees "apply is idempotent" by re-deriving the plan against live data so an already-written row returns UNCHANGED, plus a SHA-256 file-hash no-op; `RosterFeed.keyField` (EMAIL|EXTERNAL_ID, schema.prisma:3798) is the precedent for a configurable match key. What is genuinely new is the marketing key itself, and it needs schema: `Company` has NO `vatId` and NO `country` (only a free-text `address`). So this becomes: add `vatId String?` and `country String? @db.VarChar(2)` to Company with `@@unique([orgId, vatId])` (org-scoped, never global), then write the batched `resolve()` hook. Reuse `normalizeNameKey()` from /home/user/internship/src/lib/duplicateDetection.ts for the name fallback rather than a plain `toLowerCase()` — it transliterates İ/ı before lowercasing, which a German/Turkish customer list will hit on day one.

- **T-1.1.5 · `--commit` flag that writes, with a summary**  
  The CLI shell already exists: /home/user/internship/scripts/import-csv.mjs is dry-run-by-default and writes on `--apply` with `--owner=<email>`, so use that convention — do not introduce `--commit`/`--as`. `lifecycleTimestampsFor()` does NOT exist in the destination and has no home: there is no LifecycleStage enum, so trial/converted/churn date derivation is dropped from this task entirely (revisit it as its own issue if the marketing funnel really needs derived dates; StageSla at schema.prisma:240 is the destination's stage-timing mechanism). `StageChange` maps to `StatusChange` (schema.prisma:2665) in spirit but NOT in shape — it is keyed on `relationId` (MentorshipRelation), and MARKETING has no `mentorship` capability, so an account-level stage audit needs its own small table or a `companyId` variant. The script has no session, so it must take the org-explicit path the way `runRosterFeed()` does in src/lib/rosterIngestStore.ts, not `withTenantScope`.

- **T-1.1.6 · Import the primary contact per row**  
  There is no `Contact` model in the destination. The two existing shapes are a `User` with `companyId` (relation "CompanyUsers") — wrong, that creates an auth identity for someone you only want to phone — and the contact columns on `CompanyInquiry` (contactName/email/phone, schema.prisma:1438). Cheapest correct answer for v1: add `contactName`/`contactPhone` alongside the existing `Company.contactEmail` and import the primary contact INTO the Company row, deferring a `CompanyContact` table until a merchant demonstrably needs several. Also fix a latent bug carried from the source: `Contact.email` was globally `@unique`, which in a multi-tenant database leaks across tenants (see /home/user/internship/docs/tenant-isolation.md) — any uniqueness here must be `@@unique([orgId, email])`. Reuse `normalizePhoneKey()`/`normalizeEmailKey()` from src/lib/duplicateDetection.ts for the match.

- **T-1.1.8 · Document how to run an import**  
  Still wanted, but it must slot into the destination's existing docs rather than create a parallel one. /home/user/internship/docs/roster-feed.md is the established map for importers and should link the new docs/marketing-import.md. The backup warning changes target: there is no `infra/backup-db.sh` here — link /home/user/internship/docs/disaster-recovery.md. The PII warning should link the destination's own /home/user/internship/docs/pii-access-lifecycle.md and docs/DATA_ACCESS_POLICY.md instead of restating the rule. The genuinely-missing one-liner: /home/user/internship/.gitignore has no `*.csv` entry, so a real customer export can still be committed today.

### DROP (4)

- **T-1.1.2 · Parse and validate rows with a zod schema**  
  /home/user/internship/src/lib/importPreview.ts already provides `parseDelimited()` (UTF-8 BOM, CRLF, newline inside a quoted field, `;` for German/Turkish Excel, `""` escaping) plus `sniffDelimiter()` and `headerIndex()`, and `runImport()` already collects per-row failures as `{row, key, status:'SKIP'|'ERROR', reason}` into `ImportReport.rows`/`counts` instead of throwing. Adding `csv-parse` would be a SECOND parser, which /home/user/internship/docs/roster-feed.md forbids in as many words ("There is one parser and one dry-run engine in the tree"). The only residue is one `validate` callback for the marketing columns, which is folded into T-1.1.5's checklist below.

- **T-1.1.3 · Dry-run reporter: print what would change**  
  This is verbatim the contract of /home/user/internship/src/lib/importPreview.ts — its header states THE ONE RULE: "A dry run is the same call with a writer that does not write. There is no `if (dryRun)` fork in the planning path." `RowResult.changed` already carries the field names an UPDATE would touch, `emptyCounts()`/`countRows()` already produce would-create/would-update/unchanged counts, and `rosterIngest.ts`'s `previewWriter` is the working precedent. The epic's design (a separate reporter branch) is the exact bug #1432 that this engine was built to eliminate — building it would be a regression, not a feature.

- **T-1.2.1 · Admin-only CSV upload page**  
  It already ships. /home/user/internship/src/app/admin/settings/page.tsx has a CSV textarea with a Preview button posting `{csv, dryRun:true}` and an Import button posting `dryRun:false` to /home/user/internship/src/app/api/admin/import/route.ts, which checks the role server-side (`session.user.role !== 'ADMIN'` → 401, line 38) inside `withTenantScope` (line 40) and returns a per-row report. /home/user/internship/e2e/import-dryrun.spec.ts already asserts the dry run reports willCreate/skipped/errors and writes nothing. The task's own core instruction — "extract it into src/lib/import.ts first so the script and the page cannot drift apart" — is precisely what src/lib/importPreview.ts already is. The only residue is pointing that existing screen at the marketing account importer, which is a consumer of the engine and belongs to T-1.1.5.

- **T-1.2.2 · Reject files that are too large or not CSV**  
  The caps exist. /home/user/internship/src/app/api/admin/import/route.ts:13 already bounds the payload with zod (`z.string().min(1).max(200_000)`) and returns a readable 400, and /home/user/internship/src/lib/bulkInvite.ts declares `BULK_INVITE_MAX_ROWS = 5000` / `BULK_INVITE_MAX_CHARS = 500_000` as the house limits for a pasted roster. There is no MIME check to add because the screen takes pasted TEXT, not a file upload; where the destination does take files it already sniffs content signatures rather than trusting `file.type` (/home/user/internship/src/lib/fileType.ts, #888). Nothing to build.


## epic-02 — Never lose a trial

**Karar: CARRY_OVER**

Story 2.1 (the whole scheduled-job foundation) is entirely redundant: `prisma/schema.prisma:1711` already has a two-layer `Setting` model with `src/lib/settings.ts`, `src/app/api/cron/route.ts` + `src/app/api/cron/start/route.ts` are the named-job registry and the `CRON_SECRET`/`timingSafeEqual` entry point (failing closed with 503, asserted by `e2e/cron-start.spec.ts`), node-cron schedules live in `initCronJobs()` with single-owner election in `src/lib/jobs/lease.ts`, and the first-run backlog problem is already solved by `prisma/backfill-cron-baseline.mjs` + `Setting['cronBaselineAt']` run from `infra/deploy-prod.sh:708`. What is genuinely missing is the TRIAL domain itself: `grep -i trial` over `src/lib`, `src/app/api` and `prisma/schema.prisma` returns four unrelated comments, and the shipped MARKETING preset (`src/lib/programTemplates.ts:517`, `MARKETING_FUNNEL`) goes LEAD_NEW → … → DEAL_WON/DEAL_LOST with no trial stage at all. So the carry-over is: trial stages in the preset, a nullable `trialEndsAt` on the pipeline card (`MentorshipRelation`, `prisma/schema.prisma:2001` — no new table), a T-7/T-3/T-0 reminder job in the existing cron registry, and an `expire-trials` sweep modelled on `expireOffers()` (`src/lib/offerNotify.ts:119`).

### ADAPT (8)

- **T-2.1.4 · Baseline guard so the first run does not email history**  
  The MECHANISM exists — `prisma/backfill-cron-baseline.mjs` writes `Setting['cronBaselineAt']` once, self-skips on re-run, and is wired into `infra/deploy-prod.sh:708`; its header documents per-job which backlogs are and are not suppressed. So this stops being an infrastructure task and becomes one acceptance criterion on the trial-reminder job: before it is switched on, either backfill `TrialReminder` claim rows for every relation whose `trialEndsAt` is already past, or put a lower bound on `trialEndsAt` in the query, and record the choice in the backfill script's header list the way the four existing entries are. Also adopt `DORMANT_NUDGE_MAX_PER_RUN = 50` (src/services/emailService.ts:2272) as a per-tick send cap — the destination already learned that a few hundred near-identical mails in one minute gets the sending domain throttled.

- **T-2.2.1 · Reminder email template**  
  There is no `src/lib/mailer.ts` to extend and no bespoke plain-text/HTML pair to write. Sending goes through `sendEmail({ category, userId, locale, to, subject, html })` in `src/services/emailService.ts`, which handles EmailLog, the List-* headers, `brandHeader()`/`ctaBlock()` org branding and the bulk-vs-primary relay split. So the task becomes: add a `trials` copy block to `src/i18n/dictionaries.ts` in all three locales (en/tr/de — `scripts/check-i18n.ts` enforces parity), vary only the subject across the 7/3/0 thresholds as the epic asks, and reuse the EXISTING `stage-deadline` email category (already grouped under `task_reminders` in `src/lib/emailGroups.ts:164`) rather than adding to the frozen-ish `NOTIFICATION_CATEGORIES` list. No new preference key.

- **T-2.2.2 · Find the trials that need a reminder**  
  Genuinely new — nothing in the destination knows what a trial is — but it cannot query `Customer` at `TRIAL_ACTIVE_500`. The pipeline card is `MentorshipRelation` (`prisma/schema.prisma:2001`), whose `pipelineStatus` has been a free String since #747, so the query selects on the MARKETING trial stage KEY. Two prerequisites, both cheap: (a) add `TRIAL_ACTIVE` / `TRIAL_EXPIRED` stages to `MARKETING_FUNNEL` in `src/lib/programTemplates.ts:517` (today it is LEAD_NEW → LEAD_CONTACTED → LEAD_QUALIFIED → DEAL_PROPOSAL → DEAL_NEGOTIATION → DEAL_WON/DEAL_LOST, no trial anywhere), with `isOffPath: false` on TRIAL_EXPIRED so the automatic move in T-2.3.1 does not trip the drop-off-reason rule; (b) add `trialStartedAt DateTime?` and `trialEndsAt DateTime?` to `MentorshipRelation` — additive nullable columns of exactly the shape it already carries (`completedAt`, `dormantSince`, `dormantNudgeSentAt`), NO new table. Do not reuse `stageDeadline`: `src/lib/stageSla.ts` clears it on every stage move and it warns once after breach, which is an internal SLA, not a contractual end date. The calendar-day comparison and the injected `now` are the real value being carried: every existing sweep in the destination compares timestamps (`expiresAt: { lt: now }` in `src/lib/offerNotify.ts:122`, `stageDeadline: { lt: now }` at `src/services/emailService.ts:2146`) and would miss a trial ending a few hours before the tick. Unit-test it with fixed dates via the dependency-free `scripts/test/*.test.mjs` pattern (see `scripts/test/stage-aging.test.mjs`, `scripts/test/job-lease.test.mjs`) — no DB.

- **T-2.2.3 · Record what was sent, so nothing is sent twice**  
  The table is warranted but must be copied from the destination's existing claim-row pattern, not invented: `WeeklyReportReminder` (`prisma/schema.prisma:3048`), `DocumentRequirementReminder` (1222) and `MeetingSeriesReminder` (2584) are the same idea. So: `TrialReminder { id, relationId, threshold Int, sentAt }` unique on `[relationId, threshold]`, cascading from `MentorshipRelation` (not `Customer`). CONFLICT TO SETTLE IN THE PR: the epic says write the row AFTER a successful send; the destination's shipped convention is the opposite and is documented as a deliberate choice — `sendWeeklyReportReminders` (`src/services/emailService.ts:2210`) claims with `createMany({ skipDuplicates: true })` BEFORE sending, and `sendMeetingReminders` states it outright ("Marking first means a mid-send failure loses a reminder rather than duplicating one — the far less annoying failure mode", ~:2381). The trial case is arguably the one where the epic's ordering wins (a never-sent trial reminder loses a customer), but the divergence has to be argued in a comment rather than carried over silently.

- **T-2.2.4 · Wire it together as the `trial-reminders` job**  
  Not a new `POST /api/cron/[job]` — that registry exists. Add `src/lib/jobs/trialReminders.ts` following the newer convention (`src/lib/jobs/dlqAlert.ts`, `src/lib/jobs/usageRollup.ts`: a `run…()` plus an `init…Cron()`), register the timer in `src/app/api/cron/start/route.ts` next to the other four `init*Cron()` calls, and add a `job === 'trial-reminders'` branch to `src/app/api/cron/route.ts`. Recipient changes shape: there is no `Customer.assignedTo`; the owner is the relation's `mentorId` (which the MARKETING terminology overlay renders as the account owner, `src/i18n/verticalOverlays.ts:44`). The unassigned fallback must be ORG-SCOPED ADMINs, not every ADMIN on the platform — this is multi-tenant (`src/lib/orgContext.ts`). Gate the sweep per tenant with `verticalFor(orgId)` from `src/lib/verticalContext.ts`, whose header explicitly makes "on behalf of which tenant?" a required answer for out-of-request jobs. Send BOTH channels the way every sibling job does: `notifyIfAllowed(userId, 'deadlines', …)` for the bell plus `emailAllowed()` + `emailGroupAllowedForCategory()` before the mail. Per-send try/catch is already house style (see `checkStageDeadlineReminders`, `src/services/emailService.ts:2183`). Return `{ considered, sent, skipped, failed }` as the epic specifies.

- **T-2.2.5 · End-to-end test for the reminder job**  
  Half of it is already covered: `e2e/cron-start.spec.ts` asserts the unauthenticated refusal on the secret-protected endpoint as a `@smoke` test, so do not write that assertion again. The SMTP-in-CI worry is also already solved by precedent rather than a new `MAIL_TRANSPORT=stub` mode — `e2e/cron-jobs.spec.ts` runs the real job with no SMTP configured and asserts on DB state (`reminderSentAt` stamped, counts in the JSON body), because `sendEmail` failures are caught per-send. So the remaining spec is: seed a MARKETING org + a relation at the trial stage with `trialEndsAt` exactly 7 days out using `e2e/helpers/db.ts` (`seedUser`, `uniqueEmail`, `cleanupByEmail`), hit `/api/cron?job=trial-reminders` twice as an ADMIN, assert exactly one `TrialReminder` row and that the second run reports zero.

- **T-2.3.1 · Auto-advance `TRIAL_ACTIVE` past its end date**  
  Genuinely missing — the destination has no auto-advance anywhere — but the shape is already written next door and should be copied rather than reinvented: `expireOffers()` (`src/lib/offerNotify.ts:119-140`) does a conditional `updateMany({ where: { id, status: 'SENT' } })` so a human who moved the row first simply wins, then writes an `AuditLog` row with `actorId: 'system'` and notifies. Three concrete substitutions for the epic's wording: (1) there is no `lifecycleTimestampsFor()`; the destination's equivalent on a stage move is `stageDeadlineUpdate()` from `src/lib/stageSla.ts`, which sets the new stage's SLA clock and re-arms `deadlineReminderSentAt`. (2) There is no `StageChange` model; the audit row is `StatusChange` (`prisma/schema.prisma:2665`) — and note its `changedById` is a REQUIRED FK to `User`, so a system-initiated move needs a decision (expireOffers sidesteps this by writing `AuditLog` with `actorId: 'system'` instead; pick one and say why). (3) Run the move through `validateDropoffReason()` (`src/lib/stageChange.ts`) or make TRIAL_EXPIRED `isOffPath: false` in the preset — an off-path target demands a `reasonCode` that an automatic sweep has no way to supply. "Only touch rows still at TRIAL_ACTIVE" is exactly the conditional-claim guarantee, so it comes for free.

- **T-2.3.2 · Dashboard card for expired trials awaiting a decision**  
  Mostly redundant as specified: the destination's board already renders the TRIAL_EXPIRED column with a time-in-stage clock (`src/lib/stageClock.ts`, the single source of truth for "days in stage"), an amber WIP warning above `boardWipLimit` (`src/lib/boardWip.ts` + `StageSla.wipLimit`), and an aging report (`src/lib/stageAging.ts`, `/api/admin/analytics/aging`). Rather than a new card duplicating that, the cheap carry-over is one entry in the existing ranked queue: add a `'trial_expired'` value to the `AttentionReason` union in `src/lib/mentorAttention.ts:7` and a branch in `getAttentionItems()` that flags relations sitting at TRIAL_EXPIRED, so it appears in the surface an owner already scans. The epic's "hide the card when empty" rule is satisfied by construction — the queue only lists items with reasons.

### DROP (3)

- **T-2.1.1 · Add a `Setting` key/value model**  
  Already exists and is strictly better. `prisma/schema.prisma:1711` defines `model Setting { id, orgId String?, key, value @db.Text, updatedAt, @@unique([orgId, key]) }` — a two-layer (per-tenant override + global `orgId = NULL`) store, which the flat `key @id` model in the task could not express in a multi-tenant repo. `src/lib/settings.ts` exports `getSetting(key, orgId?)`, `getSettings()`, `setSetting()` and a documented `SETTING_DEFAULTS` catalogue, with the tenant auto-filter deliberately disabled so the global fallback row stays reachable.

- **T-2.1.2 · Authenticated cron endpoint**  
  Both halves already exist. `src/app/api/cron/start/route.ts` is the shared-secret endpoint: `CRON_SECRET` read from env, sent as `x-cron-secret`, compared with `timingSafeEqual`, 401 on mismatch and 503 when the secret is unset — i.e. it already fails closed exactly as the task asks. `src/app/api/cron/route.ts` is the named-job registry (`?job=weekly-reports|email-health|stage-deadlines|retention|usage-rollup|…` plus a run-everything batch), ADMIN-session gated with a 401. `e2e/cron-start.spec.ts` asserts the unauthenticated refusal as a `@smoke` test. Beyond the epic: `src/lib/jobs/lease.ts` (`JobLease`) gives the scheduler a single owner so two replicas cannot double-send. The only residue is one line registering the new job name, folded into T-2.2.4.

- **T-2.1.3 · Document how the job actually gets called**  
  The destination does not use an external timer, so a systemd unit would document a design it does not have. `src/instrumentation.ts` POSTs `/api/cron/start` at boot, node-cron holds the schedules in-process (`initCronJobs()` at `src/services/emailService.ts:3535`, plus `initNewsletterCron`/`initDeadLetterAlertCron`/`initRetentionCron`/`initUsageRollupCron`), and `src/lib/jobs/lease.ts` elects the single owner across replicas. The operator-facing documentation exists in `docs/EMAIL_DELIVERABILITY.md` (§ baseline/scheduling) and `infra/deploy-prod.sh`.


## epic-03 — See the funnel (analytics)

**Karar: MOSTLY_REDUNDANT**

The destination already ships this epic's machinery almost end to end: `src/lib/funnelKpi.ts` (stage-to-stage conversion, time-to-completion, biggest drop-off, all driven by the tenant's OWN stage order via `onPathKeys()`), `src/lib/stageAging.ts` (median/avg days per stage, unit-tested), `/api/admin/analytics{,/funnel,/aging,/cohorts,/sources}`, and a 723-line `/admin/analytics` page with CSS funnel bars, a date-range preset selector, overflow-x-auto tables, multi-sheet XLSX export, a print-friendly `/admin/analytics/report` and a weekly emailed report job — plus `e2e/analytics*.spec.ts`, `e2e/funnel-kpi.spec.ts` and `e2e/custom-pipeline-analytics.spec.ts` proving it all works on a non-default stage set. Since the MARKETING stage preset (`MARKETING_FUNNEL` in `src/lib/programTemplates.ts`) is just another org-configurable pipeline, most of this epic is already rendered for a marketing tenant on day one. What genuinely does not exist is month-cohort conversion (cohorting by entry month so late conversions do not push a rate over 100%), a retention/churn triangle with immature buckets shown as "—", and any notion of deal/recurring revenue for a TRACKED account (Subscription/plans.ts is coreCRM's own billing; roi.ts/Placement.value is placement ROI) — plus one blocking bug, the headline conversion tile still hardcoding `HIRED_660` (#1882).

### ADAPT (7)

- **T-3.1.2 · Trial -> paying conversion rate by month**  
  The cohort-by-entry-month idea is a real gap, but nothing about it survives as written. Destination has stage-to-stage conversion (stageConversions() in src/lib/funnelKpi.ts) and monthly trend buckets (trends.newRelations in src/app/api/admin/analytics/route.ts), and a per-Cohort-row comparison (/api/admin/analytics/cohorts) — none of which cohorts conversions by the month of ENTRY. Becomes: add a pure `conversionByEntryMonth(order, journeys, fromKey, toKey, months)` to src/lib/funnelKpi.ts (same dependency-free style, unit-tested next to stageConversions), cohorting on the month a journey first entered `fromKey` and counting a conversion whenever it LATER reached `toKey`. No LifecycleStage enum, no TRIAL_ACTIVE_500/CUSTOMER_ACTIVE_700 literals: the keys come from the org's own resolved order (LEAD_QUALIFIED -> DEAL_WON for MARKETING_FUNNEL). Reads StatusChange, not MentorshipRelation.pipelineStatus — the destination already builds `Journey[]` from statusChanges in src/app/api/admin/analytics/funnel/route.ts, so reuse that mapping. No new model.

- **T-3.1.4 · Conversion by source and by campaign**  
  Source half largely exists: src/app/api/admin/analytics/sources/route.ts + src/components/admin/SourceConversion.tsx already report per-`Source` intake vs converted, with an explicit `unsourced` count — exactly the 'No campaign' reasoning the task asks for. Campaign does NOT exist: there is no Campaign model in prisma/schema.prisma (only public-page UTM events in src/lib/track.ts, which write nothing to the DB). So: reuse the existing `Source` model (orgId, name, contactName/contactEmail, User.sourceId, plus the merged referrer pointer in src/lib/referrer.ts) as marketing lead attribution — do NOT add a Campaign table. Three concrete deltas: (a) the route hardcodes `role: 'MENTEE'` and `HIRED = HIRED_660|EMPLOYED_700` — swap for the org's terminal on-path key from onPathKeys(); (b) it sits behind the `premiumAnalytics` setting gate (getSetting('premiumAnalytics')), so decide whether MARKETING tenants get it free; (c) `/admin/sources` is nav-gated by capability 'sourcing' in src/lib/navLinks.ts and MARKETING does not carry 'sourcing' — so either the attribution UI is reached from /admin/analytics only, or a 'lead source' surface is added under a capability MARKETING does have. Relabel via the existing overlay (src/i18n/verticalOverlays.ts), not new strings.

- **T-3.1.5 · Recurring revenue over time (MRR by month)**  
  Genuinely absent, and the most expensive item in the epic — but not as written. Nothing in the destination models revenue for a TRACKED account: `Subscription`/`OrgEntitlement`/src/lib/plans.ts are coreCRM's own billing of the tenant, and src/lib/roi.ts + `Placement.value` + `ProgramCost` are placement ROI. grep for 'mrr|recurring revenue' across src returns nothing. The SaleVali specifics do not migrate at all: PricingModel, monthlyTransactions, the 9,90 EUR flat / tiered-per-transaction table in src/lib/constants.ts and estimateMonthlyRevenue() in src/lib/lifecycle.ts are SaleVali PRODUCT pricing, not a CRM feature. Becomes: 'won-deal value over time' — add a nullable integer minor-units `value` + ISO-4217 `currency` to the pipeline record (MentorshipRelation is the row StatusChange hangs off; a rename/generic pipeline-record decision may belong to epic #2348 rather than here) and a pure `valueByMonth()` that reconstructs which deals were WON at each month end from StatusChange, never from today's pipelineStatus. Money rules are already decided in src/lib/roi.ts and must be reused verbatim: integer minor units only, never floats, round half-away-from-zero, and refuse (typed error) rather than guess when a window mixes currencies. Formatting goes through src/lib/money.ts formatEur/formatEurCents, which is hand-rolled specifically so an e2e can pin the string. Drop the 'recorded vs estimated' split — there is no estimator to be honest about once the tiered pricing table is gone.

- **T-3.2.1 · Page shell and navigation entry**  
  The literal task is done: /admin/analytics exists (src/app/admin/analytics/page.tsx, 723 lines), src/lib/navLinks.ts line 74 registers it with BarChart3 and — note — NO `capability` tag, so a MARKETING org already sees the link (filtered by filterLinksForCapabilities()), and e2e/analytics.spec.ts already asserts a signed-in admin reaches it. The real residual work is the opposite of building a shell: the page shows a marketing tenant cards that mean nothing to it — mentor workload (`data-testid=mentor-workload-card`), mentor capacity from getMentorAvailability() in the funnel route, meeting RSVP acceptance, projectWorkload counted as `interns`, and the signup funnel. Becomes: gate those cards on verticalHasCapability(vertical,'mentorship'/'placements') using the same shellCapabilities() path src/lib/capabilityGate.ts uses, and add an e2e alongside e2e/vertical-nav-gate.spec.ts asserting a MARKETING org gets the funnel + conversion + aging cards and none of the mentorship ones.

- **T-3.2.2 · Stat tiles for the headline numbers**  
  The tiles exist — the local `Stat` component in src/app/admin/analytics/page.tsx plus src/components/ui/Card, already fed totalRelations / conversionToHired / engagement / RSVP, with money handled by src/lib/money.ts. What must change is one number, and it is a known destination bug: `conversionToHired` in src/app/api/admin/analytics/route.ts is computed as (funnel.HIRED_660 + funnel.EMPLOYED_700) / totalRelations, a hardcoded key set that reports 0% for any org on its own stage catalogue. e2e/custom-pipeline-analytics.spec.ts documents this explicitly ('The headline conversion number on /admin/analytics is deliberately NOT asserted: it still compares against HIRED_660 ... #1882'). For MARKETING that means the headline tile reads 0% forever while DEAL_WON fills up. Becomes: resolve the terminal on-path key via resolvePipelineStages()+onPathKeys() and label the tile from the resolved stage, i.e. close #1882 — this is a hard dependency for the marketing funnel, not a new tile set.

- **T-3.2.4 · Conversion and revenue tables**  
  Two of the three tables exist: the stage-conversion table (entered/advanced/rate, with rate null rendered rather than a fake 0%) on /admin/analytics, and src/components/admin/SourceConversion.tsx + CohortComparison.tsx + ProgramBenchmark.tsx, all already wrapped in overflow-x-auto and repeated in the print report at src/app/admin/analytics/report/page.tsx. Only the revenue table is new, and it is blocked on T-3.1.5. Becomes: one month x value table (deals won, total value, currency) mounted on /admin/analytics behind the same premium/capability checks as its neighbours, plus a row in the exportXlsxSheets() call at src/app/admin/analytics/page.tsx:230 and a Section in the print report. Drop the 'recorded vs estimated' column pair with the estimator.

- **T-3.3.1 · Churn by cohort**  
  Real gap, generic shape. The destination's /api/admin/analytics/cohorts is keyed on the `Cohort` MODEL (a named term/intake with relations) and reports total/hired/dropped/avgDaysToHired — not a month-cohort retention triangle, and it has no maturity rule, so nothing there renders '—' for a bucket that has not aged yet. Becomes: a pure `retentionTriangle(order, journeys, months, buckets=[1,3,6,12])` in src/lib/funnelKpi.ts, cohorting on the month a journey first reached the terminal on-path key (DEAL_WON) and returning, per bucket, the share that later reached a loss stage — with `null` (never 0) for any bucket whose window has not closed, mirroring the existing convention in that file where `rate` is null when there is no rate to state. Two decisions to settle first: MARKETING_FUNNEL has no post-win churn stage at all (only DEAL_LOST, which is a pre-win loss), so either src/lib/programTemplates.ts gains a CHURNED stage for the marketing preset or churn is defined as any isOffPath arrival AFTER a DEAL_WON arrival; and the SaleVali CANCELLATION_NOTICE_800/CHURNED_900 keys do not migrate. No new model — StatusChange carries both dates.

### DROP (6)

- **T-3.1.1 · src/lib/analytics.ts skeleton and date-range helper**  
  Redundant twice over. (1) The name is already taken: /home/user/internship/src/lib/analytics.ts is the growth-analytics provider module (Plausible/GA4/PostHog snippets, consent-gated) — creating a metrics module there would collide. (2) The range plumbing exists: rangeQuery(preset) in src/app/admin/analytics/page.tsx covers 30/90/6m/12m/all, and every analytics route (src/app/api/admin/analytics/route.ts, funnel/route.ts, aging/route.ts) parses ?from=&to= with an explicit bad/inverted-date fallback rather than a 500. e2e/analytics-date-range.spec.ts pins the behaviour. Metric functions in the destination are pure and take their window as an argument already (computeStageAging's inWindow callback).

- **T-3.1.3 · Median time in each stage**  
  Already shipped and better specified. src/lib/stageAging.ts `computeStageAging()` returns per-stage `medianDays` AND `avgDays`, with `visits` vs `candidates` distinguished (a re-entry counts twice, someone who never left counts zero) and `droppedNonPositive` surfaced instead of silently discarded. Exposed by src/app/api/admin/analytics/aging/route.ts, rendered on /admin/analytics, unit-tested in scripts/test/stage-aging.test.mjs and covered by e2e/analytics-aging.spec.ts + e2e/analytics-negative-duration.spec.ts. The 'still open' half is covered separately by `oldestStuck`/`overdue`/`pooledCount` in the same route, computed through the shared src/lib/stageClock.ts so the board and the report cannot disagree.

- **T-3.2.3 · Funnel chart in plain CSS**  
  Already built exactly as specified, including the 'no chart library' constraint. src/app/admin/analytics/page.tsx renders one horizontal bar per stage with Tailwind and `style={{ width: `${(n/maxFunnel)*100}%` }}`, iterating `stages` from useResolvedStages() — so it already follows the ORG's own order and will render MARKETING_FUNNEL's LEAD_*/DEAL_* stages with their catalogue labels and colours without a line of new code. The conversion bars at line 382 do the same for rates. Drop-off is already a first-class number: biggestDropOff() in src/lib/funnelKpi.ts deliberately excludes the terminal stage. Separating outcomes from steps is also solved — PipelineStage.isTerminal/isOffPath plus PIPELINE_GROUPS/groupResolvedStages() in src/lib/pipeline.ts, and MARKETING_FUNNEL already marks DEAL_WON terminal and DEAL_LOST terminal+off-path.

- **T-3.2.5 · Date-range filter**  
  Shipped. RangePreset ('30'|'90'|'6m'|'12m'|'all') + rangeQuery() in src/app/admin/analytics/page.tsx drives every fetch on the page; each API validates ?from/?to and falls back to its default window instead of throwing (src/app/api/admin/analytics/route.ts, funnel/route.ts, aging/route.ts), and the windows are semantically careful in a way the epic asks for — the funnel route filters journeys that STARTED in the window precisely so the time-to-completion number is not biased toward fast journeys. e2e/analytics-date-range.spec.ts and e2e/mentor-analytics-range-export.spec.ts cover it. The only delta is that the preset lives in React state rather than the URL, so a view is not linkable; that is a small optional follow-up, not epic work.

- **T-3.2.6 · Export the current view as CSV**  
  Export is solved three ways already. (1) /admin/analytics has two export buttons calling exportXlsxSheets() from src/lib/excel.ts (lines 207 and 228) — multi-sheet XLSX, dynamically imported so SheetJS stays out of the main bundle, filename stamped with the date. (2) src/app/admin/analytics/report/page.tsx is a print-friendly full report producing PDF through the browser print pipeline (no server binary). (3) A weekly analytics report email job runs from src/app/api/cron/route.ts (e2e/scheduled-analytics-report.spec.ts). CSV with Content-Disposition also has a precedent if it is ever wanted (src/app/api/admin/invitations/route.ts:179). The 401-on-every-route rule the task worries about is enforced repo-wide already, and the premium reports add a 403 `feature_locked` gate on top.

- **T-3.3.2 · Aggregate churn reasons**  
  The destination went further than this task asks. src/app/api/admin/analytics/aging/route.ts returns `dropReasons` — a stage x reasonCode count for every move that landed on a stage the ORG's own pipeline marks isOffPath (never a hardcoded key list) — and rows with no reason group under UNSPECIFIED_REASON (src/lib/dropoffReasons.ts), which is exactly the 'make the missing input visible' requirement. Better still, the gap is closed at the write path rather than only reported: validateDropoffReason() in src/lib/stageChange.ts requires a whitelisted reasonCode on every off-path move and a non-empty reasonNote when the code is OTHER, across all three write paths. Rendered on /admin/analytics via the DropReason interface. One small delta worth a separate issue, not a carry-over: DROPOFF_REASON_CODES is hiring-flavoured (ACCEPTED_ELSEWHERE, SKILL_MISMATCH, PERFORMANCE) and would want a per-vertical list for MARKETING.


## epic-04 — TR/DE/EN i18n

**Karar: MOSTLY_REDUNDANT**

The destination already ships the entire epic as built infrastructure: `src/i18n/{config,dictionaries,server,client}.ts` (a 15,348-line EN/TR/DE dictionary with `Dict`-typed key parity), `User.preferredLanguage` + the `locale` cookie + `src/components/LanguageSwitcher.tsx` + persistence in `src/components/AccountSettings.tsx`, `scripts/check-i18n.ts` wired at `.github/workflows/ci.yml:41`, locale-aware mail (`src/services/emailService.ts` `resolveLocale`/`getDictionary`, `InvitationToken.locale` #1720, locale pickers at `src/app/admin/invite/page.tsx:304` and `invite/bulk/page.tsx:193`), localized enum labels (`src/lib/pipeline.ts` `stageLabel()`, `src/lib/interactionTypes.ts`), and locale-aware formatting (`src/lib/relativeTime.ts`, `src/lib/money.ts`) policed by `scripts/check-locale-dates.mjs` at `ci.yml:210`. Building any of it again would be pure duplication. What genuinely does not exist is marketing *vocabulary*: the per-vertical overlay in `src/i18n/verticalOverlays.ts` (#2354) dresses only `nav.candidates`, `candidates.title/subtitle` and the `landing` block, so a MARKETING tenant on the surfaces its own capability list grants still reads "Manage partner companies and their internship needs" (`dictionaries.ts:658`), "No mentees in this stage" (`board.emptyStage`) and "Find a mentee or mentor…" (`adminBoard.searchPlaceholder`). The whole epic therefore collapses to one carry-over: extend that overlay across companies / pipeline / projects / messaging / documents in all three locales.

### ADAPT (3)

- **T-4.2.2 · Customer list and filters**  
  There is no `Customer` model and no customer list to translate — the marketing analogue is `/admin/candidates` (already overlaid to "Leads" / "Fırsatlar" in `src/i18n/verticalOverlays.ts`, proven by `e2e/vertical-terminology.spec.ts`) and `/admin/companies`. What survives is overlay work, not translation work: add MARKETING overrides for the `companiesPage` namespace, whose `subtitle` still reads "Manage partner companies and their internship needs" (dictionaries.ts:658) and whose `mentorships`/`positions` count labels are meaningless to a marketing tenant. Reuses the existing `Company` model and the existing dictionary keys — no new table, no new strings file.

- **T-4.2.3 · Customer detail page**  
  Becomes: overlay the pipeline/board surfaces a MARKETING tenant actually sees. `board.emptyStage` is "No mentees in this stage", `adminBoard.subtitle` is "All mentees across every mentor…", `adminBoard.searchPlaceholder` is "Find a mentee or mentor..." and `adminBoard.groups` are "Pre-internship"/"Internship" (dictionaries.ts:1537-1556) — all reachable under the MARKETING `pipeline` capability. The pluralisation and embedded-constant concerns are already solved patterns here (one/other key pairs such as `a11y.resultsShown` vs `resultsShownOne`, and `interpolate()` / `{count}`/`{limit}` placeholders), so no new machinery. Stage names themselves need nothing: `MARKETING_FUNNEL` in `src/lib/programTemplates.ts:517` already ships `labels: { en, tr, de }` per stage and `stageLabel()` (src/lib/pipeline.ts:271) resolves them.

- **T-4.2.5 · Enum labels: stages, pricing, channels, sources**  
  The destination already enforces exactly this rule and states it in a comment: `src/lib/interactionTypes.ts` holds "the *keys* only — the display labels are localized in `t.interactionTypes`… Never pair a key with a hardcoded English label at a call site", and `src/lib/pipeline.ts` does the same for stages. So the retro-fit is done. What the task becomes is an acceptance criterion on the OTHER carried epics: any marketing enum they introduce (pricing model, customer source, integration channel, SEPA status) ships as a bare key array plus a `dictionaries.ts` namespace in EN/TR/DE — never a `Record<Enum, string>` — and proper nouns (Amazon, eBay, OTTO, Shopify, WooCommerce) are left identical in all three locales with a comment saying so. No standalone work item.

### DROP (7)

- **T-4.1.1 · Locale files and the t() helper**  
  Already built. `src/i18n/config.ts` defines `locales = ['en','tr','de']` with `defaultLocale='en'`; `src/i18n/dictionaries.ts` (15,348 lines) has `const en = {...}` as the reference with `type Dict = typeof en` and `dictionaries: Record<Locale, Dict>`, so a missing/misspelled key in tr/de is a `tsc --noEmit` failure exactly as the task wanted. `getDictionary(locale)` (dictionaries.ts:15333) serves server components, `useT()` / `LocaleProvider` (src/i18n/client.tsx) serve client ones, and `ClientDictionary = Omit<Dictionary, SERVER_ONLY_NAMESPACES>` keeps server-only namespaces out of the browser bundle. `{name}`/`{count}` interpolation exists as `interpolate(template, values)` in `src/lib/notificationText.ts:40` plus `.replace('{count}', …)` at call sites.

- **T-4.1.2 · Per-user locale and a language switcher**  
  Already built, and with a better resolution order than the task proposed. `User.preferredLanguage String?` (prisma/schema.prisma:423); `getLocale()` in `src/i18n/server.ts` reads the explicit `LOCALE_COOKIE` first, then the signed-in user's `preferredLanguage`, then the default — and short-circuits on `hasSessionCookie()` so a public page costs no query (#1197). `src/components/LanguageSwitcher.tsx` is the device-level switcher (mounted in `AccountMenu.tsx` and `landing/PublicHeader.tsx`); the durable per-user choice is written from `AccountSettings.tsx:348` via `PATCH` of `preferredLanguage`. The Marketing note about not conflating operator language with the tracked party's language is already modelled: `preferredLanguage` is the UI locale and schema.prisma:297 documents the separate mentor-spoken-languages field.

- **T-4.1.3 · CI check that the three files agree**  
  `scripts/check-i18n.ts` exists and does more than the task asked: it flattens all three dictionaries, reports `[tr] missing key: …` and `[de] extra key: …` in both directions, AND fails on empty/whitespace-only translations. It additionally validates the vertical overlays (every override must target an existing base key; INTERNSHIP must override nothing). Exposed as `npm run check:i18n` and run in CI at `.github/workflows/ci.yml:41`.

- **T-4.2.1 · Sidebar, login and register**  
  No equivalent work left. The destination nav is fully translated under the `nav` and `panel` dictionary namespaces (dictionaries.ts:6-68) and the auth pages under `auth` (dictionaries.ts:1402), error strings included; `src/app/auth/signin/SignInClient.tsx`, `register/page.tsx`, `forgot/page.tsx` and `reset/page.tsx` all read the dictionary. `e2e/i18n.spec.ts`, `e2e/i18n-de.spec.ts` and `e2e/i18n-coverage.spec.ts` already guard this. For a MARKETING tenant the irrelevant nav entries are removed by the capability gate (#2351, `e2e/vertical-nav-gate.spec.ts`), not by translation.

- **T-4.2.4 · Dashboard, contacts and campaigns**  
  The destination dashboard is already translated (`dashboard` namespace, dictionaries.ts:422) including stat tiles and empty states, and `e2e/i18n-coverage.spec.ts` exists precisely to catch hardcoded English on admin screens. Standalone "contacts" and "campaigns" screens do not exist in the destination and are not part of this epic — if the carried-over acquisition/attribution epic adds them, they are written against `dictionaries.ts` from the first commit and the `Dict` type plus `check:i18n` make an English-only screen impossible to merge. There is no retro-translation backlog to carry.

- **T-4.2.6 · Emails in the recipient's language**  
  Fully shipped as #1720. `src/services/emailService.ts` resolves a locale per send (`resolveLocale(locale)` + `getDictionary(resolved)`) for the invitation, password-reset, verification and footer paths, subject lines included; the header comment states "LOCALE is REQUIRED thinking, not an optional nicety". The invitee-has-no-account case is solved the way the task proposed: `InvitationToken.locale String?` (prisma/schema.prisma:2709) stores the inviter's choice so a later RESEND arrives in the same language, and the picker exists at `src/app/admin/invite/page.tsx:304` and `src/app/admin/invite/bulk/page.tsx:193`. Templates live in the same dictionary as the UI (`notifications.invitationEmail` etc.), not a second mechanism. Covered by `e2e/system-mail-language.spec.ts` and `e2e/system-mail-i18n.unit.spec.ts`.

- **T-4.3.1 · Locale-aware dates and numbers**  
  Solved, guarded, and with a regression story attached. `src/lib/relativeTime.ts` exports `formatDate(date, locale, options?)`, `formatDateTime`, `formatTime` and `formatDateTimeWithZone` (which also names the timezone); `src/lib/money.ts` exports `formatEur`, `formatEurCents`, `formatPercent`, `formatDecimal`, `formatCount`, all taking `Locale`. `scripts/check-locale-dates.mjs` fails CI on any bare `toLocaleDateString()`/`toLocaleTimeString()`/`toLocaleString()` — its header documents #1422, the exact bug this task was written to prevent — and runs at `.github/workflows/ci.yml:210`, with `e2e/locale-dates.unit.spec.ts` and `e2e/date-formatter-locale.spec.ts` on top.


## epic-05 — Operations — Deploy it and keep it running

**Karar: MOSTLY_REDUNDANT**

Epic 5 is entirely "stand up a server and keep it alive" for a deployment that never ran — and the destination is a live, mature deployment that already has every piece: a self-hosted runner with its own watchdog (.github/workflows/runner-watchdog.yml), /etc/internship-crm/prod.env plus a documented variable list and HEALTH_TOKEN contract (infra/README.md, src/app/api/health/route.ts), Caddy ingress with automatic TLS and a wildcard runbook (infra/README.md §Caddy, infra/acme-issue-wildcard.sh), a nightly 03:15 UTC backup cron with a daily freshness check and monthly restore drill (infra/backup-db.sh, infra/check-backups.sh, infra/restore-drill.sh, .github/workflows/backup-verify.yml), an executed disaster-recovery runbook (docs/disaster-recovery.md), external uptime monitoring off-box (.github/workflows/uptime.yml), version + preview chip already in the UI (src/lib/version.ts → RoleShell.tsx, src/lib/appEnv.ts → BetaBadge.tsx), and a sharded e2e suite that already merges shard reports into one Turkish email with the very E2E_REPORT_MODE switch this epic asks for (.github/workflows/e2e-full.yml, scripts/e2e-report-email.mjs). The single piece of real work left is the one the destination itself flagged as outstanding: CHANGELOG.md #2355 says "the DNS/proxy cutover of that live domain is a separate, deliberate infra step" — marketing.ersah.in must be repointed at the Caddy box, given a site file and MARKETING_HOSTS in prod.env, so src/lib/hostVertical.ts actually resolves MARKETING in production.

### ADAPT (1)

- **T-5.1.3 · Reverse proxy and TLS**  
  Ingress itself is a DROP: Caddy on the destination box terminates TLS and issues certificates itself, one /etc/caddy/sites/<fqdn>.caddy per host (infra/README.md §2166 migration note, infra/server/topic-deploy.sh:86 CADDY_SITES_DIR). What survives is the MARKETING-vertical cutover that CHANGELOG.md #2355 explicitly deferred: point marketing.ersah.in (currently served by the retiring marketing box, and on a different apex than the *.interncrm.com wildcard) at the Caddy host, add a marketing.ersah.in.caddy with reverse_proxy 127.0.0.1:3200, and set MARKETING_HOSTS in /etc/internship-crm/prod.env (and the preview host in preview.env) so src/lib/hostVertical.ts resolves MARKETING in production instead of only by default.

### DROP (8)

- **T-5.1.1 · Provision the self-hosted runner**  
  The destination already runs on a self-hosted runner and has for months — .github/workflows/runner-watchdog.yml SSHes in every 15 minutes from a GitHub-hosted runner to restart svc.sh if it died, and infra/README.md documents the box (Caddy host, containers internship-crm:3200 / internship-crm-preview:3201). infra/infra-setup.yml and infra/server/bootstrap.sh cover provisioning. Nothing to register; the marketing repo's runner is being deleted with it.

- **T-5.1.2 · Create the production env file**  
  /etc/internship-crm/prod.env already exists, is read by infra/deploy-prod.sh, by the backup cron in infra/README.md (line 531: 'set -a; . /etc/internship-crm/prod.env'), and its variable list is documented there. HEALTH_TOKEN is already a first-class concept with a documented fail-open contract in src/app/api/health/route.ts. The only surviving fragment — adding MARKETING_HOSTS — is folded into T-5.1.3 rather than duplicated here.

- **T-5.1.4 · Rehearse on preview first**  
  deploy-preview.yml runs continuously against https://preview.interncrm.com on every push to main and every 6h (infra/README.md:440), with the backup, schema-guard and post-swap health check already exercised on every run. There is no first deploy to rehearse. The only rehearsal that still matters — checking the marketing landing on a preview host before the live DNS flip — is a done-when of the T-5.1.3 ADAPT.

- **T-5.2.1 · Nightly backup on a timer**  
  infra/README.md §'Daily backup (cron)' already installs '15 3 * * * … internship-backup-db --env prod' against /etc/internship-crm/prod.env, writing to /var/backups/internship-crm with KEEP_DAYS pruning. Silent failure is covered better than the task asked: .github/workflows/backup-verify.yml runs a daily freshness check via infra/check-backups.sh with the alert job deliberately on a GitHub-hosted runner, 'because an alert that needs the failing server to be healthy is an alert that may never send'.

- **T-5.2.2 · Prove a restore actually works**  
  docs/disaster-recovery.md exists, is written as an executed runbook (dump selection, docker stop, gzip -dc | mysql, deploy-prod.sh --no-pull, health check), covers both full loss and post-bad-edit recovery, and refuses to quote invented timings — the measured RPO/RTO come from infra/restore-drill.sh, which is automated and fired monthly by backup-verify.yml against a guarded scratch database.

- **T-5.2.3 · External uptime monitoring**  
  .github/workflows/uptime.yml polls the public environments every 5 minutes from GitHub-hosted runners — explicitly moved off-box after the 2026-09-03 outage took the on-server monitor down with it — and alerts on state change via an 'uptime-alert' issue so an outage sends one mail, not twelve an hour. It uses no token, matching the task's anonymous-shape requirement.

- **T-5.2.4 · Show the running version in the UI**  
  src/lib/version.ts exports APP_VERSION (derived, with a package.json fallback) and GIT_SHA (baked into the image, 'dev' locally with no special-casing), and src/components/RoleShell.tsx:105 already passes version={APP_VERSION} into the shell sidebar. The preview marker exists too: src/lib/appEnv.ts IS_PREVIEW drives src/components/BetaBadge.tsx, which renders the label 'preview' instead of 'beta'.

- **T-5.2.5 · Email a summary after the scheduled full E2E run**  
  scripts/e2e-report-email.mjs already merges every shard JSON from a directory (with E2E_EXPECTED_REPORTS catching a crashed shard) into one Turkish message, and .github/workflows/e2e-full.yml wires it to a 4×/day 4-way sharded run. The exact switch the task specifies exists and has already been flipped: red-only is the default since 2026-08-23, E2E_REPORT_MODE=always restores the '✅ N/N test geçti' heartbeat. An unset ALERT_EMAIL_TO is a skip, not a failure (e2e-report-email.mjs:176).


## epic-06 — Access / roles — Access control and data protection

**Karar: MOSTLY_REDUNDANT**

Bu epic'in altyapı yarısı destination'da zaten var: fail-closed kapsamlama `src/lib/authzScope.ts` (+ `docs/role-access-matrix.md`), tablo tabanlı negatif E2E `e2e/authz-matrix.spec.ts` + `e2e/fixtures/authz-matrix.ts`, hiç throw etmeyen erişim logu `src/lib/activity.ts` → `ActivityLog` (schema.prisma:2918) ve ayarlanabilir saklama/IP temizliği `src/lib/retentionEntries.ts` (`activityLogRetentionDays`, varsayılan 365), GDPR silme `src/lib/accountErasure.ts` + `src/app/api/admin/users/[id]/erase/route.ts`, kişisel veri/yedek politikası `docs/pii-access-lifecycle.md` + `docs/trust/*` + `docs/disaster-recovery.md`. Geriye kalan gerçek iş tek bir yerde toplanıyor: `Company` hiçbir rol kapsamına tabi değil — `GET /api/companies` ve `GET /api/companies/[id]` yalnızca `if (!session)` kontrol ediyor, `withTenantScope` dışında filtre yok, yani MENTEE/SOURCE/COMPANY dahil her oturum tenant'taki tüm firmaları (`contactEmail` ile birlikte) okuyor; MARKETING dikeyinde `Company` merkezî kayıt olduğu için bu kabul edilemez. Ayrıca `CompanyInquiry.contactName/email/phone/message` (schema.prisma:1438) hiçbir silme yoluna uğramıyor ve firma bazlı JSON dosya dışa aktarımı yok (`src/app/api/account/export/route.ts` yalnızca kullanıcının kendi verisi).

### KEEP (1)

- **T-6.2.4 · Export everything about one customer**  
  Firma bazlı dosya dışa aktarımı yok. Mevcut olan tek JSON dışa aktarım src/app/api/account/export/route.ts ve o yalnızca oturum sahibinin KENDİ verisi (GDPR self-service); src/lib/excel.ts + e2e/export.spec.ts aday listesinin .xlsx dökümü, tek kayıt dosyası değil. src/app/api/companies/[id]/ altında sadece route.ts var. Marketing alanına özgü iş olarak taşınır — ancak `Customer` yerine `Company`, ve yeni tablo gerektirmez: mevcut ilişkiler üzerinden toplanır (Company + needs + requisitions + offers + interests + CompanyInquiry + firmanın ilişkileri üzerinden InteractionLog (schema.prisma:2373, relationId'ye bağlı) + StatusChange (schema.prisma:2665, StageChange'in karşılığı)).

### ADAPT (6)

- **T-6.1.1 · Decide and write down the access matrix**  
  Yeni doküman yazılmayacak: /home/user/internship/docs/role-access-matrix.md zaten var (154 satır, fail-closed gerekçesi + relation/project tabloları, kaynak kod referansı src/lib/authzScope.ts). Bu görev, o dokümana bir 'MARKETING dikeyi' bölümü eklemeye dönüşür: kapsanan yeni kaynak `company`, ve cevaplanacak soru yeniden yazılmalı — Role enum'u DONUK (ADMIN|MENTOR|MENTEE|COMPANY|SOURCE, schema.prisma:18), MARKETER/MANAGER yok, dolayısıyla soru 'marketer sadece kendi müşterilerini mi görür' değil, 'operatör olmayan roller (COMPANY/MENTEE/SOURCE) firma kayıtlarını hiç görmeli mi, MENTOR kendi ilişkili firmalarıyla mı sınırlı olmalı'dır. Ayrıca dikeyin yetenek kısıtı (src/lib/verticals.ts: MARKETING = projects, companies, pipeline, messaging, documents) ile rol kapsamının FARKLI katmanlar olduğu tabloda belirtilmeli (src/lib/capabilityGate.ts).

- **T-6.1.2 · One place that decides scope (scopeForUser / src/lib/access.ts)**  
  Yeni `src/lib/access.ts` AÇILMAYACAK — src/lib/authzScope.ts tam olarak bu modül: `scopeForRole(user, resource)`, rol başına builder, tanımsız rol için `null` (→ 403), `NO_MATCH` sentinel'i ve `logScopeDenial()`. Görev, `ScopeMap`'e üçüncü bir kaynak eklemeye indirgenir: `company: Prisma.CompanyWhereInput`, BUILDERS.company altında ADMIN `{}`, COMPANY `{ id: u.companyId ?? NO_MATCH }`, MENTOR ilişkili firmalar (`{ mentorships: { some: { mentorId: u.id } } }`), MENTEE/SOURCE ya `{ id: NO_MATCH }` ya da builder yok (403). `canEdit/canDelete` ayrı fonksiyon olarak gerekmiyor: yazma yolları zaten `session.user.role !== 'ADMIN'` ile korunuyor (src/app/api/companies/route.ts POST) ve modül yazma için değil okuma kapsamı için tasarlandı. Mevcut birim test dosyasının yanına rol başına deny testi eklenir.

- **T-6.1.3 · Route every query through it**  
  GERÇEK AÇIK — DROP değil. src/app/api/companies/route.ts GET yalnızca `if (!session)` kontrol edip `withTenantScope` içinde `prisma.company.findMany({ include: { needs, _count } })` çalıştırıyor; src/app/api/companies/[id]/route.ts GET de aynı. Yani her oturum tenant'taki tüm firmaları `contactEmail` ile okuyor. Görev, T-6.1.2'de eklenen `company` kapsamını bu iki route'a (+ firmayı id üzerinden dolaylı okuyan needs/requisitions/offers/interests uçlarına) uygulamaya dönüşür. Marketing'in '403 değil 404 döndür' kuralı OLDUĞU GİBİ taşınmaz: bu repo'nun konvansiyonu, kapsamı tanımsız rol için `logScopeDenial()` + 403 (src/lib/authzScope.ts başlığı, e2e/authz-idor.spec.ts cross-user CV için 403 bekliyor); kapsam İÇİNDE bulunamayan satır için 404. Yeni kural icat etmek yerine bu ayrım koda yorum olarak yazılmalı. `Customer` yok — hedef model `Company` (schema.prisma:1267).

- **T-6.1.4 · Authorization matrix e2e spec**  
  Yeni `e2e/authz-matrix.spec.ts` YAZILMAYACAK — aynı isimde spec zaten var ve tam olarak istenen şeyi yapıyor: 5 rolü seed ediyor, ikinci bir 'öteki taraf' dünyası kuruyor, `e2e/fixtures/authz-matrix.ts` içindeki MATRIX'ten tablo tabanlı yürüyor ve `own` hücrelerinde status değil dönen HER SATIRIN çağırana ait olduğunu doğruluyor. Yanında e2e/authz-idor.spec.ts, e2e/role-scoping.spec.ts, e2e/company-role.spec.ts var. Kalan iş: MATRIX fixture'ına `GET /api/companies` ve `GET /api/companies/:id` satırlarını rol başına eklemek ve `own` iddiasını firma kimliği üzerinden kurmak.

- **T-6.2.1 · Log who read which customer (AccessLog model)**  
  Yeni `AccessLog` TABLOSU AÇILMAYACAK. Üç şart da karşılanmış durumda: (1) model — `ActivityLog` (prisma/schema.prisma:2918) actorId/targetType/targetId/ip/userAgent ile; (2) 'logging asla sayfayı kırmasın' — src/lib/activity.ts `logActivity()` insert'i try/catch içinde, yorumu birebir aynı gerekçeyi taşıyor; (3) hacim ve saklama — src/lib/retentionEntries.ts `activityLog` girdisi, ayar anahtarı `activityLogRetentionDays` (src/lib/settings.ts, varsayılan 365), batch'li budama + kanıt niteliğindeki action'larda satırı koruyup ip/userAgent'i temizleme. Desen e2e/document-access-log.spec.ts ile, kullanıcıya gösterimi e2e/account-access-history.spec.ts ile kanıtlı. Kalan iş tek satır: firma detayı açıldığında `logActivity({ action: 'company.view', targetType: 'Company', targetId, actorId, request })` çağrısı ve gerekiyorsa aynı kullanıcı için kısa pencerede tekrar yazımı bastırmak.

- **T-6.2.2 · Erase a contact without losing the company**  
  Yeni admin aksiyonu/onay diyaloğu/denetim kaydı sıfırdan YAZILMAYACAK: src/lib/accountErasure.ts (`anonymizeUser` / `hardDeleteUser`) + src/app/api/admin/users/[id]/erase/route.ts (ADMIN şartı, impersonation reddi, isim onayı, admin'in kendi parolasıyla step-up, `logActivity`) + e2e/candidate-erasure.spec.ts, e2e/erasure-free-text.spec.ts bu görevin tarif ettiği her şeyi zaten yapıyor; hatta 'kişinin YAZDIĞI içerik tombstone, HAKKINDA yazılan serbest metin scrub, kalan satırlar kurumun operasyonel geçmişi' ayrımı accountErasure.ts'te yorumlanmış. Marketing'in `Contact` modeli bu repo'da YOK; bir firmadaki kişinin en yakın karşılığı `CompanyInquiry` (schema.prisma:1438: contactName, email, phone, message, note) ve `Company.contactEmail`. Görev şuna dönüşür: accountErasure.ts'in silme yüzeylerine `CompanyInquiry` ve `Company.contactEmail` eklenir — bugün hiçbir silme yolu bunlara dokunmuyor (grep: accountErasure.ts içinde `companyInquiry` geçmiyor), yani dönüştürülmüş bir firmanın Company satırı ve ticari geçmişi korunurken kişinin adı/e-postası/telefonu enquiry satırında kalıyor. Bu tam olarak 'kişiyi sil, firmayı koru' vakası ve gerçek bir boşluk.

### DROP (1)

- **T-6.2.3 · Data-protection policy document**  
  Tamamen mevcut. İstenen içerik dört dosyaya dağılmış ve hepsi koda referanslı: docs/pii-access-lifecycle.md (461 satır — hangi veriye kim, NE KADAR SÜRE erişir; POST_MENTORSHIP_ACCESS_MONTHS=6, src/lib/retention.ts / cvAccess.ts / documentAccess.ts'e bağlı), src/lib/retentionEntries.ts (tablo tablo saklama süreleri ve her birinin gerekçesi), docs/trust/hosting-and-residency.md + docs/trust/security-overview.md + docs/disaster-recovery.md (YEDEKLER: /var/backups/internship-crm, her prod deploy öncesi + günlük 03:15 UTC, yapılandırılabilir saklama), docs/trust/subprocessors.md (veri kategorileri ve işleyenler). 'Kim okuyabilir' sorusu tekrarlanmıyor, docs/role-access-matrix.md'ye işaret ediyor — görevin istediği yapı bu. DİKKAT: destination'daki docs/DATA_ACCESS_POLICY.md aynı ada sahip ama BAŞKA bir belgedir (katkıda bulunanların gerçek veriye erişim yasağı); üzerine yazmayın.


## epic-07 — Workflow — daily work in the CRM

**Karar: MOSTLY_REDUNDANT**

Six of the thirteen tasks are pure UI-plumbing the destination already solved better than the Marketing backlog planned to: `src/components/ui/Toast.tsx` + `LiveRegion.tsx` + `src/lib/apiErrorMessage.ts` cover save feedback, `src/components/ui/EmptyState.tsx` is a per-role empty-state component with exactly the reasoning T-7.3.4 asked for, 16 `loading.tsx` files plus `src/components/ui/Skeleton.tsx`/`src/components/PageSkeleton.tsx` cover T-7.3.5, `src/app/todos/page.tsx` + `src/components/todos/MyTodos.tsx` + `/api/todos` already are the tasks page, `src/app/api/cron/route.ts` + `sendDailyActivityDigests()`/`sendWeeklyMentorDigests()` in `src/services/emailService.ts` already are the digest (with locale, opt-out and empty-skip), and `CompanyForm` + `PATCH /api/companies/[id]` already make the "customer" editable, so the epic's one Urgent item is moot. What genuinely survives is account-level marketing work: there is no contact-person model at all (`Company` carries only a `contactEmail` string, schema line 1274), no sorting anywhere (`grep searchParams.get('sort')` returns nothing; `src/app/api/companies/route.ts:48` hardcodes `orderBy: { name: 'asc' }`), no pagination on the companies list specifically, no "assigned to me" filter, no bulk owner-assign action in `src/app/api/admin/candidates/bulk/route.ts`, and no `dueDate` on `ProjectTask` so task-level overdue cannot exist. All of those reuse existing models (`Company`, `MentorshipRelation.mentorId`/`pipelineStatus`, `ProjectTask`, `StatusChange`) except contacts, which is the epic's only justified new table.

### KEEP (1)

- **T-7.3.2 · Edit and delete contacts**  
  The only task in this epic with no destination equivalent at all. There is no contact-person model: `Company` carries a single `contactEmail String?` (schema 1274) and nothing else; `CompanyInquiry` (schema 1438) has `contactName`/`email`/`phone` but is an inbound enquiry record that is explicitly never deleted and is converted once into a Company, not a contact book; and company-side people are `User` rows (`users User[] @relation("CompanyUsers")`). Several people at one account, with a primary among them, is a genuine marketing-CRM need the destination cannot express. Model decision: do NOT reuse `User` — it is the auth principal (password, lockout, invitations, GDPR erasure, and a Role compared as a string literal in 958 places), and minting login-less `User` rows for merchant contacts would drag all of that in. Add one small `CompanyContact` model (companyId, name, email, phone, position, isPrimary, timestamps, `@@index([companyId])`) with `/api/company-contacts` CRUD behind `requireCapability(orgId, 'companies')` (`src/lib/capabilityGate.ts`). Carry over both hard-won rules from the source prompt: reassigning a contact to a different company must be supported (people change employer; re-creating loses the record), and marking one contact primary must unmark the previous primary for that company IN THE SAME TRANSACTION or the UI shows two.

### ADAPT (6)

- **T-7.1.2 · Sort the customer list**  
  Genuinely missing — `grep "searchParams.get('sort')"` over src returns zero hits; `src/app/api/companies/route.ts:48` hardcodes `orderBy: { name: 'asc' }` and `src/app/api/candidates/route.ts:147` hardcodes `createdAt: 'desc'`. But the sort keys do not migrate: there is no PricingModel, no `monthlyTransactions` and no trial window in the destination, so 'trial ending soonest' and 'highest revenue' have nothing to sort on. Becomes: a `sort` query param on the companies/pipeline list offering name A–Z, recently created, last stage move (newest `StatusChange.createdAt`) and longest in stage (via `src/lib/stageClock.ts` `daysInStage`). Keep the source task's real lesson verbatim — a row with no stage move must sort LAST, which MySQL will not do by default, so the Prisma `orderBy` needs an explicit `{ sort: 'asc', nulls: 'last' }`.

- **T-7.1.3 · Paginate the list**  
  Pagination machinery is not new work — the `page` + `pageSize` + `total` pattern is in ~11 places (`src/app/admin/candidates/page.tsx:70-72`, `src/app/admin/users/page.tsx:73`, `src/app/admin/offers/page.tsx:28`, `src/app/notifications/page.tsx:26`, `src/app/company/talent-pool/page.tsx:26`, …). But the COMPANIES screen is the one that never got it: `src/app/api/companies/route.ts` returns every row and `src/app/admin/companies/page.tsx:88-93` filters client-side with `companies.filter(...)`. Becomes: apply the existing candidates pagination pattern to `/api/companies` + the companies grid (server-side `search`, `skip`/`take`, `total` in the same call), and move the search box server-side while there. No new helper — copy the shape from `/api/candidates`.

- **T-7.1.4 · "My customers" quick filter**  
  No 'assigned to me' filter exists anywhere (grep for `mine=1` / `assignedToMe` / `onlyMine` returns nothing); today a mentor sees only their own rows because `/mentor/mentees` scopes by `mentorId`, and the admin grid has no such toggle at all. The `Customer.assignedTo` column does not migrate: `Company` has no owner column. Becomes: a `mine=1` boolean param on the pipeline/companies list resolving to `MentorshipRelation.mentorId = session.user.id` (the destination's existing deal-owner field — `@@index([pipelineStatus])` and `mentorId` are already there), composing with the existing `status`/`tags`/`search`/`source` filters in `buildFilterParams()` (`src/app/admin/candidates/page.tsx:159-180`) rather than replacing them. Do NOT add an owner column to Company for this.

- **T-7.1.5 · Bulk assign an owner**  
  Half of it already exists: `src/app/admin/candidates/page.tsx` has per-row checkboxes (`selected: Set<string>`), a bulk bar and `POST /api/admin/candidates/bulk` (`src/app/api/admin/candidates/bulk/route.ts`) with actions `activate | deactivate | advanceStage | addTag | removeTag`, a 200-id cap, per-target eligibility filtering and `logActivity()`. Per-row owner assignment also exists (`src/components/admin/AssignMentorInline.tsx`). Missing is only the bulk variant. Becomes: add an `assignOwner` action (plus `ownerId`) to the existing bulk endpoint's zod `bodySchema` and a control to the existing selection bar — no new route, no new selection machinery. Carry over the source task's two judgements: report `N reassigned`, and check eligibility per row rather than once for the batch (the existing addTag branch already sets that precedent with its per-person cap).

- **T-7.2.2 · Highlight overdue tasks**  
  The destination has a rich overdue vocabulary, but all of it is about STAGE service levels, not tasks: `src/lib/stageClock.ts`, `StageSla.days`, `src/components/StageClockChip.tsx` (`overdue: 'text-red-700 bg-red-50 border-red-200'`), `src/components/MentorAttentionQueue.tsx` (reason `'overdue'`), `CalendarView.tsx` deadline chips, and the `stage-deadlines` cron job calling `checkStageDeadlineReminders()`. Task-level overdue cannot exist because `ProjectTask` (schema 1833-1869) has NO `dueDate` — only `done`, `doneAt`, `archivedAt`, `assigneeId`. `Goal.dueDate` exists (schema 2360) but `src/components/GoalsPanel.tsx` renders it with no overdue treatment. Becomes: add one nullable `dueDate` to `ProjectTask`, sort `/api/todos` by it ascending nulls-last, render the overdue label + tone in `src/components/todos/TodoRow.tsx` reusing `StageClockChip`'s colour tones, and add the count tile. Keep both source rules: overdue means before TODAY by calendar day (a task due today is not late at 09:00), and never colour alone — add a text label, which the repo's a11y gate (`e2e/a11y-scan.spec.ts`, `e2e/a11y-baseline.json`) will hold you to anyway. Use `DAY_MS` from `src/lib/stageClock.ts` / `src/lib/relativeTime.ts`, not `daysUntil()` from the retired `src/lib/lifecycle.ts`. Add the manager/team view here as a `scope=team` param on `/api/todos`.

- **T-7.3.1 · Delete a customer from the UI**  
  The route exists and is reachable: `DELETE /api/companies/[id]` (`src/app/api/companies/[id]/route.ts:113`, ADMIN-only, inside `withTenantScope`), wired to a trash button on the companies grid. What is missing is precisely the part the source task cared about: `src/app/admin/companies/page.tsx:137-141` uses the browser's native `confirm()` with only the company name, while the repo ships a real `src/components/ui/ConfirmDialog.tsx` (already used by `MyTodos.tsx`). Becomes: swap `window.confirm` for `ConfirmDialog`, and have the delete path first fetch the dependent counts so the dialog can state consequences concretely — `Company` cascades to `CompanyEntitlement` and `Requisition` (`onDelete: Cascade`) while `MentorshipRelation.companyId`, `Project` and `CompanyInquiry.convertedCompanyId` survive by `SetNull`; say which is which. Role note: the Role enum is FROZEN, so 'MANAGER and ADMIN' is just ADMIN here — the existing route check is already the policy, do not invent a role.

### DROP (6)

- **T-7.1.1 · Edit an existing customer**  
  The destination's marketing 'customer' is Company, and it is already fully editable. `src/app/admin/companies/page.tsx` holds an `editingCompany` state and renders the SAME `src/components/forms/CompanyForm.tsx` for create and edit (the source task's 'extract the shared form rather than copying it' is already how it is built), backed by `PATCH /api/companies/[id]` with `updateCompanySchema` (`src/app/api/companies/[id]/route.ts:9-28`). The epic's one Urgent item — 'the most basic CRUD operation is missing' — is simply not true in the destination. The stage-exclusion rule it wanted also already holds: stage lives on `MentorshipRelation.pipelineStatus` and is written only through `src/lib/stageChange.ts` + `emitStageChange()`, never through the company form.

- **T-7.2.1 · A tasks page**  
  `src/app/todos/page.tsx` + `src/components/todos/MyTodos.tsx` + `GET/POST /api/todos` already are this page: everything assigned to the signed-in user in one list, toggled in place via `PATCH /api/project-tasks/[id]`, with an archive view, its own `loading.tsx` and `error.tsx`, and a sidebar entry. Reading someone else's list is already handled by `src/components/todos/PersonTodos.tsx`, mounted on `src/app/admin/candidates/[id]/page.tsx:566` and `src/app/mentor/mentees/[id]/page.tsx:554`, with the authorization rule written out in `/api/todos` ('an ADMIN may, and a MENTOR may for their own mentee'). The two unmet bullets — due-date ordering and a team-wide filter — are folded into T-7.2.2 rather than kept as a separate page task.

- **T-7.2.3 · Daily digest of what is due**  
  Every 'Done when' bullet is already satisfied. The cron registry is `src/app/api/cron/route.ts` with named, individually runnable jobs (`weekly-reports`, `email-health`, `stage-deadlines`, `meeting-logs`, `re-engagement`, `newsletters`, `newsletter-queue`, `dormant`, `dlq-alert`, `retention`, `usage-rollup`, `missing-documents`) plus a run-everything path. The digests themselves are `sendDailyActivityDigests()` and `sendWeeklyMentorDigests()` in `src/services/emailService.ts`, which already do the three things the source prompt argued for: per-recipient locale (`getDictionary(mLocale)`), an opt-out gate (`emailAllowed(m, 'digest')` + `emailGroupAllowedForCategory(m, 'activity-digest')`), and skipping recipients with nothing to say ('Recipients with no mentees / no data are skipped'). The idempotency guard the source wanted also exists per-job (`deadlineReminderSentAt`, `stalenessReminderSentAt`). Residue: once T-7.2.2 gives `ProjectTask` a `dueDate`, adding a due/overdue block to the existing daily digest is a few lines inside `sendDailyActivityDigests()` — an extension of that task, not a task of its own.

- **T-7.3.3 · Confirmation feedback on save**  
  `src/components/ui/Toast.tsx` (`useToast(message, 'error' | …)`) is the one shared component the source task asked someone to build, and it is already used consistently — `MyTodos.tsx`, `ProfileForm.tsx`, `OfferCard.tsx`, `OfferManagementPanel.tsx`, `NotesPanel.tsx`, `ContactActions.tsx`, `MeetingLauncher.tsx`, `OfferWizardModal.tsx`, `ApiExplorer.tsx`, `MenteeActivationPanel.tsx`. It is paired with `src/components/ui/LiveRegion.tsx` (`useAnnounce`) so the confirmation reaches a screen reader too, and with `src/lib/apiErrorMessage.ts`, which exists specifically to avoid the failure the source task described (a hardcoded English `body.error` shown centre-stage to a Turkish admin). The `?error=validation` silent-redirect problem is an artefact of the Marketing repo's server actions and does not exist here.

- **T-7.3.4 · Empty states that point somewhere**  
  `src/components/ui/EmptyState.tsx` is a mature component that already encodes more than the source task asked for: `title` + one-sentence `body` + icon + a single next-step `action`, plus per-role copy and per-role actions (`byRole`), a `size` for inline slots, and a `data-testid="empty-<testId>"` hook for the e2e suite. Its header comment states the same principle the source prompt argued ('Every screen with nothing in it either teaches or disappoints') and adds one the Marketing backlog missed — a role with no entry gets the explanation and NO button, 'which is what keeps this component from ever offering a route the viewer would be refused by'. Already mounted on `src/app/admin/companies/page.tsx` and `src/components/todos/MyTodos.tsx`. New marketing screens should use it; nothing needs building.

- **T-7.3.5 · Loading states for slow pages**  
  16 route-level `loading.tsx` files already exist (`src/app/admin/loading.tsx`, `src/app/todos/loading.tsx`, `src/app/company/loading.tsx`, `src/app/mentors/loading.tsx`, `src/app/messages/**/loading.tsx`, `src/app/portal/loading.tsx`, …), and the skeleton vocabulary the source task wanted instead of a spinner is `src/components/ui/Skeleton.tsx` (`SkeletonRows`, used inside `MyTodos.tsx`) plus `src/components/PageSkeleton.tsx`. Any new marketing route gets a `loading.tsx` as a one-line convention, not as backlog work.


## epic-08 — Quality — Test coverage and guardrails

**Karar: MOSTLY_REDUNDANT**

Six of the seven tasks build infrastructure the destination already has and has hardened further: the unit harness plus a per-file coverage ratchet (`scripts/check-unit-coverage.mjs`, `npm run test:unit`, 30 files in `scripts/test/`, wired at `.github/workflows/ci.yml:322`), the full invitation lifecycle e2e (`e2e/invitations.spec.ts`, `e2e/invite-lifecycle.spec.ts`, `e2e/invitation-board.spec.ts`), role gating (`e2e/authz-matrix.spec.ts` + `e2e/fixtures/authz-matrix.ts`, `e2e/authz-idor.spec.ts`, and for the vertical specifically `e2e/vertical-nav-gate.spec.ts` / `e2e/vertical-write-gate.spec.ts`), and a three-layer axe gate (`e2e/a11y.spec.ts` @smoke, `e2e/a11y-scan.spec.ts` over 15 screens, `e2e/a11y-baseline.json`). What genuinely survives is a MARKETING demo tenant — `prisma/seed-demo.mjs:348` upserts exactly one Organization with no `vertical`, so the marketing funnel preset (`src/lib/programTemplates.ts:517` MARKETING_FUNNEL) has no demo data behind it — and one guard the destination really lacks: nothing statically asserts that the 248 `route.ts` files under `src/app/api/**` check a session (`scripts/check-api-key-routes.mjs` only covers `/api/v1` + `withApiKey`, `scripts/check-auth-reads.mjs` only covers column-scoped reads in the auth path).

### ADAPT (3)

- **T-8.1.3 · E2E for campaigns**  
  Not writable today and must not become a standalone issue: the destination has NO Campaign model. `grep -ri campaign prisma/schema.prisma src` finds only UTM string literals in src/lib/demoMode.ts and src/lib/track.ts. The nearest existing model, `Source` (prisma/schema.prisma:667), is partner-institution referral intake and belongs to the `sourcing` capability, which MARKETING explicitly does NOT have (src/lib/verticals.ts). So this becomes an acceptance criterion folded INTO whichever epic ports marketing acquisition attribution — 'creating an account against campaign X makes X's account count read 1' as a test in that feature's own spec — rather than a test task of its own. If campaign attribution is not carried over, this task is void.

- **T-8.1.5 · Demo seed script**  
  The script exists (prisma/seed-demo.mjs, 783 lines, `npm run seed:demo`), is idempotent (@demo.example.com namespace), refuses non-local targets via assertSafeDemoTarget() in prisma/demoTarget.mjs, and is ratcheted by scripts/check-demo-fidelity.mjs + scripts/demo-fidelity.json in CI — so 'write a demo seeder' is done. What is missing is the MARKETING half: line 348 upserts ONE Organization and never sets `vertical`, so every demo row is an INTERNSHIP tenant and the shipped marketing funnel preset (MARKETING_FUNNEL, src/lib/programTemplates.ts:517, VERTICAL_DEFAULT_TEMPLATE at :565) has nothing behind it. Becomes: seed a second demo Organization with vertical:'MARKETING', provisioned through defaultTemplateForVertical() so its PipelineStage rows are LEAD_NEW..DEAL_LOST, ~30 German merchant accounts on the EXISTING Company model (prisma/schema.prisma:1267 — no new table), spread across those seven stages with InteractionLog rows (:2373) and BACKDATED stage history so stageAging/analytics has a curve to chart, then add the new models to scripts/demo-fidelity.json. Blocked on one open decision owned by the pipeline epic: which entity carries the marketing stage (today only MentorshipRelation.pipelineStatus, schema :2021, sits on the board).

- **T-8.2.1 · Check that every API route checks the session**  
  The one task in this epic with real residual value — the destination genuinely lacks it. 248 route.ts files live under src/app/api/**, and the only static guards are scripts/check-api-key-routes.mjs (scoped to src/app/api/v1 and withApiKey()) and scripts/check-auth-reads.mjs (column-scoped User reads on the sign-in path only). .eslintrc.json carries no such rule. Becomes a generic repo guard, not marketing work: scripts/check-route-auth.mjs + `npm run check:route-auth` in .github/workflows/ci.yml, reusing the routeFiles() walker from check-api-key-routes.mjs, failing when an exported handler contains no getServerAuthSession()/auth-guard call. Because the codebase is large and pre-existing, ship it with a frozen baseline file (the pattern used by scripts/stage-keys-baseline.json and e2e/a11y-baseline.json) rather than a green-on-day-one allowlist, plus a reasoned exception list: /api/health, /api/auth/[...nextauth], /api/register, /api/invite/opened, /api/cron/*, /api/v1/** (covered by withApiKey), /api/v1/openapi.json, unsubscribe and inbound-email webhooks.

### DROP (4)

- **T-8.1.1 · Unit-test harness for src/lib**  
  The destination already has exactly this harness and more. `npm run test:unit` = `node --test --experimental-strip-types scripts/test/*.test.mjs` (package.json), 30 suites in scripts/test/ (verticals, money, plans, metering, stage-aging, rate-limit-store, roi...), each wired individually into .github/workflows/ci.yml, plus a per-file coverage RATCHET the source repo never had: scripts/check-unit-coverage.mjs with a FLOORS map (e.g. src/lib/verticals.ts floor 95) run as `npm run test:unit:coverage` at ci.yml:322, documented in docs/testing.md 'Unit tests and the coverage floor (#1599)'. Nothing to build: any marketing pure module a later epic ports just adds scripts/test/<module>.test.mjs and a FLOORS entry. Note also that the two things this task wanted covered do not exist in the destination to cover: SaleVali's tiered pricing has no counterpart (src/lib/money.ts and src/lib/plans.ts are coreCRM's own SaaS pricing), and the lifecycle date derivation is replaced by the per-org PipelineStage machinery (prisma/schema.prisma:264, src/lib/stageAging.ts, already unit-tested in scripts/test/stage-aging.test.mjs).

- **T-8.1.2 · E2E for the invitation and registration flow**  
  Fully covered, and by the auth/invitation system that actually survives the migration. e2e/invitations.spec.ts (create, list, resend, cancel via /api/invite), e2e/invite-lifecycle.spec.ts (sent -> openedAt -> registeredAt/used=true -> verifiedAt, asserted against prisma.invitationToken), e2e/invitation-board.spec.ts (expired status rendering), plus e2e/invite.spec.ts, invite-link-only, invite-company-hint, invite-mentor-confirm, invite-autolink-referral, bulk-invite.spec.ts and register-org.spec.ts. The `seedInvite()` helper in e2e/helpers/db.ts is the 'seed the token, never touch SMTP' pattern this task asked for. Marketing's own InvitationToken/mailer is discarded with the repo.

- **T-8.1.4 · E2E for the users page and role gating**  
  Redundant twice over. (1) The Role enum is FROZEN — there is no MARKETER role to gate, so the literal task cannot exist. (2) The 'hiding a link is not access control' assertion is already the destination's house style: e2e/authz-matrix.spec.ts (238 lines, role x endpoint matrix driven by e2e/fixtures/authz-matrix.ts, asserting row OWNERSHIP not just status codes), e2e/authz-idor.spec.ts, e2e/idor-hardening.spec.ts, e2e/role-scoping.spec.ts, e2e/users-company-filter.spec.ts. For the vertical specifically it already shipped: e2e/vertical-nav-gate.spec.ts asserts a MARKETING org's sidebar loses Mentors/Mentorships/Mentor Applications/Offers/Sources, and e2e/vertical-write-gate.spec.ts covers the server-side half. Residue only: a new MARKETING screen adds a row to e2e/fixtures/authz-matrix.ts as part of that screen's own issue.

- **T-8.2.2 · Accessibility smoke check**  
  The destination's axe gate is strictly larger and already solves the 'a gate that fails on day one gets disabled in a week' problem the task worries about. e2e/a11y.spec.ts runs @axe-core/playwright on /auth/signin tagged @smoke, critical-only, in the PR gate; e2e/a11y-scan.spec.ts scans 15 role-specific screens with wcag2a..wcag22aa, fails new serious/critical and reports moderate/minor; e2e/a11y-baseline.json freezes existing violations (regenerated with A11Y_UPDATE_BASELINE=1); e2e/a11y-ci-gate.unit.spec.ts tests the gate itself; plus board-a11y, global-search-a11y, a11y-media-preferences, accessibility-statement specs, `npm run check:contrast` + `npm run test:contrast-guard` in ci.yml, and docs/testing.md 'Accessibility regression gate (axe)'. Residue only: when MARKETING screens exist, add them to the a11y-scan page list and re-baseline as part of those screens' issues.


## epic-09 — Signals from the SaleVali product / Ürün sinyalleri

**Karar: CARRY_OVER**

The epic's *core* — a per-account daily usage series, a volume-drop churn signal, and the "an ingest must never move a record backwards in the funnel" rule — genuinely does not exist in internship: there is no per-entity daily metric table (UsageRollup at prisma/schema.prisma:3664 is per (org, metric, month) billing metering), no sparkline anywhere (grep -i sparkline: zero hits, and package.json carries no chart library), and every existing "at risk" signal (src/lib/mentorAttention.ts, dormantFirstContact.ts, stageAging.ts, reEngagement.ts) is contact-recency based, never volume based. Everything *around* that core is already built and must not be rebuilt: the cron registry and named-job convention (src/app/api/cron/route.ts, src/lib/jobs/usageRollup.ts, jobs/lease.ts, dlqAlert.ts, the Job queue with idempotencyKey), the overlap-window re-fetch doctrine, the shared-secret-with-timingSafeEqual endpoint pattern (src/app/api/webhooks/jaas/route.ts, inbound-email/route.ts, cron/start/route.ts), the external-feed engine with file-hash no-op, diff, chunking and resume (src/lib/rosterIngest.ts + importPreview.ts + ssrfGuard.ts + externalSyncPolicy.ts), the person matcher (src/lib/duplicateDetection.ts), an idempotent inbound-lead→Company conversion (CompanyInquiry at prisma/schema.prisma:1438 + /api/company-inquiry + /api/admin/company-inquiries/[id]/convert), and the whole Playwright harness. Every task therefore survives as ADAPT with its scaffolding stripped out: Customer→Company, LifecycleStage→the shipped MARKETING_FUNNEL preset (src/lib/programTemplates.ts:517), and every path tenant-scoped through verticalFor()/requireCapability().

### KEEP (1)

- **T-9.1.5 · Churn-risk filter**  
  The one task with no counterpart in the destination. Every 'needs attention' signal there is recency-of-contact based and none reads a numeric series: src/lib/mentorAttention.ts (AttentionReason = inactive | overdue | unanswered_question | pending_meeting | no_open_goal | missing_weekly_reports), src/lib/dormantFirstContact.ts, src/lib/lastContact.ts, src/lib/reEngagement.ts, src/lib/stageAging.ts. A trailing-30-vs-previous-30 volume drop is a new kind of signal. Carry it over, but express it as an additional attention reason / list filter over Company + CompanyUsage rather than a bespoke /dashboard/customers query, and reuse two patterns already in mentorAttention.ts: the excluded-rows footnote (it returns dormantCount so a filtered queue never looks broken — do the same for accounts with under 60 days of history) and the tunable threshold (reminderDays comes from src/lib/settings.ts getSetting(), so the 40% and the 60-day floor should be a Setting, tunable per tenant without a deploy, rather than a frozen constant).

### ADAPT (7)

- **T-9.1.1 · Agree the data contract with the SaleVali team**  
  Still the gate for the rest of the epic — nothing in internship knows SaleVali exists — but most of the doc's open questions are already decided by house convention, so it shrinks to a short doc that points at them. docs/roster-feed.md is the template (same shape: transport, key column, what may be overwritten, tenancy, what is NOT in the engine). Three answers are pre-made: (1) credentials never live in a DB row — RosterFeed.credentialEnvVar (prisma/schema.prisma:3842) stores the NAME of the env var; (2) a tenant-supplied URL is fetched only through assertPublicHttpsUrl() (src/lib/ssrfGuard.ts); (3) 'who may overwrite what the operator typed' is already answered once for the whole tree by src/lib/externalSyncPolicy.ts (gap-fill vs authoritative) and must not get a second answer. Identity narrows too: the destination keys external feeds on a non-unique, tenant-scoped external id (User.externalId + @@index([orgId, externalId]), prisma/schema.prisma:448/517), and the contract is per MARKETING organization, not 'the CRM'. The data-protection paragraph does not get a bespoke section: it becomes a new row in docs/trust/subprocessors.md plus an entry in docs/DATA_ACCESS_POLICY.md. Destination file: docs/marketing-vertical/salevali-usage-feed.md.

- **T-9.1.2 · CustomerUsage model and external id**  
  Customer does not exist; the tracked account is Company (prisma/schema.prisma:1267), which today has name/description/contactEmail/industry/size/address/quota and NO externalId. Two changes: (a) add Company.externalId String? @db.VarChar(191) with @@index([orgId, externalId]) — NOT @unique as the epic asks. The destination has already ruled that shape out twice in writing: User.externalId (prisma/schema.prisma:441-448) and Job.idempotencyKey (:3300-3306) both record that MySQL treats NULLs in a unique index as distinct, so a composite unique over a nullable pair silently stops de-duplicating anything while every row is still null. (b) add CompanyUsage { orgId, companyId, date, transactions, orders? } @@unique([companyId, date]) @@index([companyId, date]). UsageRollup (:3664) is the house ledger shape to copy but is NOT reusable — it is per (org, metric, calendar-month) billing metering, written absolutely by the nightly rollup, and a per-company per-day product series is a different grain. Two requirements the original backlog could not know: register the model in TENANT_MODELS (src/lib/orgContext.ts) so tenant isolation scopes it, and add a retention entry (src/lib/retentionEntries.ts) — an unbounded fast-growing daily table is exactly the PageView lesson written into prisma/schema.prisma:558-563.

- **T-9.1.3 · Nightly sync job**  
  The job harness is entirely redundant; only the SaleVali-specific fetch+upsert survives. Already provided: the cron registry and the 'named job runnable on its own' convention (src/app/api/cron/route.ts, see the usage-rollup branch), the secret-protected trigger (src/app/api/cron/start/route.ts:30, timingSafeEqual), leader leases (src/lib/jobs/lease.ts), the durable queue with idempotencyKey and a dead-letter alert (Job model + src/lib/jobs/dlqAlert.ts), and the overlap-window doctrine itself — src/lib/jobs/usageRollup.ts already re-reads 'the open month and the one before it' for exactly the reason the epic argues. SSRF-safe fetching, file-hash no-op, diff, chunking and crash-resume all exist in src/lib/rosterIngest.ts + src/lib/importPreview.ts. What remains: a salevali-usage handler that, for each MARKETING org with a feed, pulls the last 7 days of per-account counts and upserts CompanyUsage; registers as a named job in the cron route; reports unmatched external ids as a count plus a sample the way rosterIngest reports `absent`; and adds a ConnectorId to src/lib/integrationHealth.ts so a feed that quietly died shows on /admin/operations. Drop the hand-written overwrite rule: 'refresh the derived figure but never overwrite an explicitly recorded one' IS planFieldUpdates() in src/lib/externalSyncPolicy.ts — call it. Note also that Company carries neither monthlyTransactions nor mrr today (grep returns nothing), so that clause depends on the epic-01/02 carry-over landing first. Gate on verticalFor(orgId) === 'MARKETING'.

- **T-9.1.4 · Usage sparkline on the customer record**  
  Genuinely missing — grep -i sparkline over src/ returns nothing, package.json dependencies contain no charting library, and only four files in the tree contain inline <svg> (Button.tsx, MatchScoreBreakdown.tsx, page.tsx, global-error.tsx). So 'no chart library' is already the house rule rather than a constraint to argue for. But the house rendering convention is NOT an SVG polyline: the existing trends chart is CSS-height divs inside a fixed BAR_AREA_PX track with per-bar data-testid hooks (src/app/admin/analytics/page.tsx:111 and :539-583), pinned by e2e/analytics-trends.spec.ts. Follow that, or state why SVG is better. Two destination facts change the task: there is no admin company DETAIL page at all (only src/app/admin/companies/page.tsx, a list, plus /api/companies/[id]), so this needs a host surface and depends on the epic-07 carry-over; and the aria-label requirement stops being advice — e2e/a11y-scan.spec.ts + e2e/a11y-baseline.json enforce it. All labels must resolve through the per-vertical terminology overlay (#2354), not hardcoded English.

- **T-9.2.1 · Ingest endpoint**  
  Two thirds of this is already shipped. The shared-secret-compared-in-constant-time endpoint is a four-times-repeated pattern to copy verbatim, not design: src/app/api/webhooks/jaas/route.ts:43, src/app/api/inbound-email/route.ts:23, src/app/api/cron/start/route.ts:30, src/app/api/health/route.ts:68 — and scripts/openapi-generate.cjs:655 already recognises that shape when generating the spec. zod validation and per-IP rate limiting (enforceRateLimit, src/lib/rateLimit.ts) are standard. More importantly, an inbound lead intake that converts idempotently into a Company already exists: CompanyInquiry (prisma/schema.prisma:1438-1473) + public POST /api/company-inquiry + POST /api/admin/company-inquiries/[id]/convert, whose idempotence is a conditional UPDATE on convertedCompanyId: null returning 409 already_converted. So the task becomes a machine-to-machine sibling of that path, not a new /api/customers/ingest with its own world. Concretely: TRIAL_ACTIVE_500 / source WEBSITE_TRIAL becomes a pipelineStatus key from the shipped MARKETING_FUNNEL preset (src/lib/programTemplates.ts:517 — note the preset has LEAD_NEW…DEAL_LOST and no trial stage, so the mapping is a decision to record); lifecycleTimestampsFor() + StageChange become the existing writer in src/lib/stageChange.ts / stageChangeEffects.ts writing a StatusChange; and the 'dedicated system user for createdById' problem disappears — the destination's session-less writes resolve a TENANT instead (defaultOrgId(), and host→vertical resolution already shipped in src/lib/hostVertical.ts, #2355). Add requireCapability(orgId, 'companies') (src/lib/capabilityGate.ts). If an API key is preferred over a shared secret, src/lib/apiScopes.ts was explicitly shaped so that adding companies:write is a list entry rather than a format migration.

- **T-9.2.2 · Make ingest idempotent**  
  Split it: the dedupe half is mostly redundant, the no-regression half is the valuable carry-over. Dedupe — do NOT create src/lib/matching.ts; that filename is already taken by the next-action heuristic, and the shared matcher exists as src/lib/duplicateDetection.ts (normalizeEmailKey / normalizePhoneKey / normalizeNameKey, transliteration-safe, bounded Levenshtein, detection-only) sitting behind the `resolve` port of runImport({parse, validate, resolve, apply}) in src/lib/importPreview.ts. It is person-oriented today, so the work is a company-level resolver beside it that both the CSV-import carry-over and the ingest call. Reuse CompanyInquiry's conditional-UPDATE claim as the idempotence mechanism. No-regression — this is real and missing: the destination has no guard stopping a write from moving a record to an earlier stage, and PipelineStage.order (prisma/schema.prisma:273) is the numeric ordering to compare. Put the rule in src/lib/stageChange.ts so every write path inherits it rather than only the ingest, and log when a regression is refused.

- **T-9.2.3 · End-to-end test for ingest**  
  The harness half is a straight DROP: Playwright is configured (playwright.config.ts, ~200 specs in e2e/), the Prisma verification helper the epic asks for exists as e2e/helpers/db.ts, and sign-in/tenant helpers exist as e2e/helpers/auth.ts and e2e/helpers/tenants.ts. The 'an unauthenticated call is refused' case is also largely covered generically by e2e/authz-matrix.spec.ts and e2e/api-v1-key-enforcement.spec.ts. What is worth writing is the two assertions nothing there makes: the same payload posted twice yields exactly one Company and exactly one StatusChange, and a signup naming a company already at DEAL_WON leaves its stage alone. Must run tenant-scoped via e2e/helpers/tenants.ts against a MARKETING-vertical org, with a unique prefix and afterAll cleanup.
