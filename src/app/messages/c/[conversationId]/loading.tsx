import { ConversationSkeleton } from '@/components/PageSkeleton';

// A direct or group conversation — same chat shape as the mentorship thread.
export default function ConversationLoading() {
  return <ConversationSkeleton />;
}
